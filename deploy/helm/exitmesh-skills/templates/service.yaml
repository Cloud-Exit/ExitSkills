apiVersion: v1
kind: Service
metadata:
  name: {{ include "exitmesh-skills.fullname" . }}
  labels:
    {{- include "exitmesh-skills.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "exitmesh-skills.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP

